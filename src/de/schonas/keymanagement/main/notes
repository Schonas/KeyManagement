###Client

TODO:
addPage, editPage Abfangen wenn nicht alles ausgefüllt ist -> Schlüssel auch ohne Ablaufdatum?
SQL Table als Konstante
Scanned signature as link
Dokuemnt über Abgabe/Annahme erstellen (Unterschriftenfeld, Keyinfos, etc)
CSS der verschiedenen Teile gleich halten, d.h. menubar und toolbar einheitlich gestalten
inventory page überschrift
edit/add Button möglichkeit für 1+ jahr geben
Inventory: läuft diesen Monat ab
wo wird die config gespeichert
Add Room/Department?

WIP/COULD BE NICER:
Clear Table if keyType has no Access (muss noch gecheckt und dann ausgeführt werden, methode ist schond da)
test con in settings
Reload Progress Bar
Expired? Col (View) in Tooltip, also as Column?
con state with icon, remove after 2 secs

ADDED/FIXED:
Save Button on Settings Page(-> Erfolg-Alert?)
hide edit on double click
zuletzt ausgewählter Schlüssel manuell erfassen -> bei klick auf add dann auf remove wird er nicht mehr erkannt
cancel button edit/add(double click)
uid keyinfo
difDays fixen!
Date fancy in table, Datum als String in DB
Date until Expired Col (View) addded as tooltip
load mysql data from config
Remove Confirm Window
Reload from Database
search
Load table after start
Icon as res (add in MainPage, Error on Edit, AddPage, Settings)
onHover show days until expiration
Add/Remove/Edit Icons
Wenn Hauptfenster geschlossen wird auch alle anderen Schließen


###Server

5min loop, check for keys -> yes? -> mail (form settings)
config datei
get smtp server and username, password, ...